steps:
  - label: ":python: Python client tests"
    commands:
      - "cd mockserver-client-python"
      - "python3 -m venv .venv"
      - ".venv/bin/pip install -e '.[dev]'"
      - ".venv/bin/pytest"
    timeout_in_minutes: 10
