---
description: Investigate AWS infrastructure issues affecting Buildkite build agents
---
Use the Agent tool to spawn a `debugger` subagent to investigate AWS infrastructure for the following request:

$ARGUMENTS

The debugger agent should read `.opencode/skills/aws-investigation/SKILL.md` and follow its investigation workflow.
