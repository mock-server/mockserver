import java.io.*;

File logFile = new File( basedir, "mockserver.log" );

if (!logFile.isFile()) {
    throw new Exception( "Mock Server log file [" + logFile + "] was not create" );
} else {
    logFile.delete();
}

return true;