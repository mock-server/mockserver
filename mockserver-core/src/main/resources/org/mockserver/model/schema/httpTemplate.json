{
    "title": "template to generate response / request",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "delay": {
            "$ref": "#/definitions/delay"
        },
        "templateType": {
            "enum": [
                "JAVASCRIPT",
                "VELOCITY"
            ]
        },
        "template": {
            "type": "string"
        }
    },
    "definitions": {
    }
}
