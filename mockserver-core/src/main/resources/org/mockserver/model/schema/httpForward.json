{
    "title": "host and port to forward to",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "host": {
            "type": "string"
        },
        "port": {
            "type": "integer"
        },
        "scheme": {
            "enum": [
                "HTTP",
                "HTTPS"
            ]
        }
    }
}
